<div class="menu-item">
      <label for="phone">رقم الجوال:</label>
      <input type="text" id="phone" name="phone" placeholder="05xxxxxxxx" required pattern="05[0-9]{8}" />
    </div>
    <button type="submit">اطلب الآن عبر واتساب</button>
  </form>
</div>

<script>
  document.getElementById('orderForm').addEventListener('submit', function(e) {
    e.preventDefault();
    let items = [];
    const brost4Count = parseInt(document.getElementById('brost4').value);
    if (brost4Count > 0) items.push(`بروست 4 قطع × brost4Count ={brost4Count*18} ريال`);
    const brost2Count = parseInt(document.getElementById('brost2').value);
    if (brost2Count > 0) items.push(`بروست 2 قطع × brost2Count ={brost2Count*10} ريال`);
    const musahab10Count = parseInt(document.getElementById('musahab10').value);
    if (musahab10Count > 0) items.push(`مسحب 10 قطع × musahab10Count ={musahab10Count*17} ريال`);
    const zingerCount = parseInt(document.getElementById('zinger').value);
    if (zingerCount > 0) items.push(`برجر زنجر × zingerCount ={zingerCount*10} ريال`);
    const burgerCount = parseInt(document.getElementById('burger').value);
    if (burgerCount > 0) items.push(`برجر عادي × burgerCount ={burgerCount*7} ريال`);